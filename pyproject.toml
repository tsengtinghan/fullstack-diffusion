[tool.poetry]
name = "fullstack-diffusion"
version = "0.1.0"
description = "fastapi backend"
authors = ["Michael Tseng <michael08120@gmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
